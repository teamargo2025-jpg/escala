{
  "name": "escala",
  "version": "0.1.0",
  "private": true,
  "description": "Guía paso a paso para armar presupuesto, precio, meta de ventas y flujo de caja.",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test src/lib/calc.test.js"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.3.0"
  }
}
